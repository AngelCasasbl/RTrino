% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbplyr-simulate.R
\name{simulate_trino}
\alias{simulate_trino}
\title{A Trino connection that never talks to a server}
\usage{
simulate_trino()
}
\value{
A simulated \code{dbplyr} connection of class \code{TrinoConnection}.
}
\description{
Returns an object that carries the \code{TrinoConnection} class without holding a
session, so \code{\link[=sql_dialect.TrinoConnection]{sql_dialect.TrinoConnection()}} and, through it,
\code{\link[=sql_translation.sql_dialect_trino]{sql_translation.sql_dialect_trino()}} dispatch as they would on a live
connection. Useful for inspecting the SQL a \code{dplyr} pipeline produces, and
used by this package's own translation tests, which then need no coordinator
at all. Anything that reaches the database, such as \code{collect()}, still fails.
}
\examples{
if (requireNamespace("dbplyr", quietly = TRUE)) {
  con <- simulate_trino()
  dbplyr::translate_sql(as.numeric(x), con = con)
}
}
\keyword{internal}
