% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssl.R
\name{trino_ssl}
\alias{trino_ssl}
\title{TLS options for a Trino connection}
\usage{
trino_ssl(verify = TRUE, ca_bundle = NULL)
}
\arguments{
\item{verify}{Whether to verify the server's certificate. Defaults to \code{TRUE}.}

\item{ca_bundle}{Path to a PEM file holding the certificate authorities to
trust, or \code{NULL} to use the system store.}
}
\value{
A list of TLS options.
}
\description{
Collects the TLS settings applied to every request made on a connection.
Pass the result as the \code{ssl_options} argument of \code{\link[DBI:dbConnect]{dbConnect()}}.
}
\details{
A cluster whose certificate is signed by an internal authority does not need
verification turned off: point \code{ca_bundle} at the authority's PEM file and
certificates keep being checked. \code{verify = FALSE} accepts any certificate,
including an attacker's, and so warns every time it is applied.
}
\examples{
# Secure default
trino_ssl()

# Trust an internal certificate authority
\dontrun{
trino_ssl(ca_bundle = "/etc/ssl/certs/internal-ca.pem")
}
}
