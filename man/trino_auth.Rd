% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{trino_auth}
\alias{trino_auth}
\alias{trino_auth_basic}
\alias{trino_auth_jwt}
\alias{trino_auth_oauth2}
\title{Authentication methods for Trino}
\usage{
trino_auth_basic(user, password)

trino_auth_jwt(token)

trino_auth_oauth2(client_id, client_secret, token_url, scope = NULL, ...)
}
\arguments{
\item{user}{User name.}

\item{password}{Password. Read it from the environment
(\code{Sys.getenv("TRINO_PASSWORD")}) rather than writing it in a script.}

\item{token}{A JWT, as a string.}

\item{client_id}{OAuth2 client id.}

\item{client_secret}{OAuth2 client secret.}

\item{token_url}{Token endpoint of the OAuth2 provider.}

\item{scope}{Optional OAuth2 scope.}

\item{...}{Further arguments passed on to
\code{\link[httr2:req_oauth_client_credentials]{httr2::req_oauth_client_credentials()}}.}
}
\value{
A function of one argument (an \code{httr2} request) returning a modified
request.
}
\description{
Each helper returns a \emph{closure} that takes an \code{httr2} request and returns it
with the appropriate credentials attached. The credentials live only in that
closure's environment, so they are never written to the global environment
and never stored on the connection object in plain sight. Pass the result as
the \code{auth} argument of \code{\link[DBI:dbConnect]{dbConnect()}}.
}
\details{
\itemize{
\item \code{trino_auth_basic()} — HTTP basic authentication, used by Trino's LDAP and
password-file authenticators.
\item \code{trino_auth_jwt()} — a bearer JWT, for service accounts and unattended
pipelines, or for a cluster fronted by an SSO proxy that mints its own
tokens.
\item \code{trino_auth_oauth2()} — OAuth2 client credentials, for corporate identity
providers such as Okta or Entra ID. \code{httr2} caches the token and renews it
when it expires.
}

Trino rejects basic and bearer credentials over plain HTTP, so use an
\verb{https://} host with all three.
}
\examples{
\dontrun{
con <- DBI::dbConnect(
  Rtrino::Trino(),
  host    = "https://trino.example.com",
  port    = 443,
  user    = "analyst",
  catalog = "hive",
  schema  = "default",
  auth    = trino_auth_basic("analyst", Sys.getenv("TRINO_PASSWORD"))
)
}
}
