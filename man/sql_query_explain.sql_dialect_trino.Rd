% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbplyr-db.R
\name{sql_query_explain.sql_dialect_trino}
\alias{sql_query_explain.sql_dialect_trino}
\title{Explain a Trino query}
\usage{
sql_query_explain.sql_dialect_trino(con, sql, ...)
}
\arguments{
\item{con}{A Trino SQL dialect object.}

\item{sql}{A query.}

\item{...}{Unused, for compatibility with the generic.}
}
\value{
The \code{EXPLAIN} statement, as SQL.
}
\description{
Trino's \code{EXPLAIN} takes the statement directly, without the parenthesised
options some engines require.
}
\details{
Column discovery (\code{sql_query_fields()}) is deliberately left to dbplyr's
default, which wraps the query in \verb{WHERE 0 = 1}. On Trino that plans without
reading any data, exactly like the \verb{LIMIT 0} the specification suggests, and
the default is built from dbplyr internals that a backend should not reach
into.
}
\keyword{internal}
